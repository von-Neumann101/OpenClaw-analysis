OpenClaw源自Claude Code，去年的热点——大模型，今年——agent

把本地（私域数据）不同来源的的数据，加上字符串匹配搜索，向量相似度搜索（语义），图谱关系搜索——RAG（类似搜索引擎）

Matrix——打通不同的Channel（开发多渠道通信系统）

Pi Runtime
Session Binding——把数据和ID对齐
Prompt Assembly——组装提示词（内置prompt）
AI Model Calling，小模型理清程序，大模型处理具体问题
Tool Invocation——Skill（调用外部工具），Plugins，hook(Web hook)，node（对接硬件代理——二次开发）
Memory Aggregation

![[Pasted image 20260422205420.png]]
Skill——经验

OpenRouter——管理各种AI模型的使用

内部信息（冲程内的，比如Session Binding）使用Plugins
![[Pasted image 20260422210155.png]]![[Pasted image 20260422210200.png]]
ACP：控制IDE

QMD——处理记忆系统最火的工具（长期短期，敏感信息）

OpenClaw是**龙虾池**，GateWay——一个路多个车（实际上是串联）

Mattermost——多个Agent像会议室一样在里面开会，人和AI们对话
